# crear usuario
adduser eduardo

# Agregar usuario al grupo de sudo
usermod -aG sudo vik

# camobiar de usuario
su - vik

# To run a command as administrator (user "root"), use "sudo <command>".
# See "man sudo_root" for details.
# exit regresamos a root
exit
